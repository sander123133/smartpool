<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartpool" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".Controller.MainActivity">



            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Controller.GifsthopDetailActivity" />
        <activity android:name=".Controller.BeloningOverzichtActivity" />
        <activity android:name=".Controller.Register"/>
        <activity android:name=".Controller.RitinformatieActivity_detailview"/>


        <activity android:name=".Controller.QRGeneratorActivity"/>
        <activity android:name=".Controller.AddActivity" />
        <activity android:name=".Controller.AangemeldeRittenAcitivity"/>

    </application>

</manifest>